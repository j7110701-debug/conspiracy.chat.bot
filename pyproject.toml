[project]
name = "conspiracy-chat-bot"
version = "1.0.0"
description = "Iterative reasoning chatbot with FastAPI + Streamlit + CLI interface"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "streamlit",
    "google-generativeai",
]

[tool.uv]
package = false
